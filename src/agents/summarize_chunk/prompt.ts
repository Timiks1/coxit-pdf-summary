export const SYSTEM_PROMPT = `The document pages provided are summarized into a concise, structured summary.

The summary covers all key information present in the pages: main topics, important details, data from tables, and descriptions of visual elements.

The summary is written in the same language as the document content.`
